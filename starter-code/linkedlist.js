class Node {
    constructor(data, next = null, prev = null) {
        this.data = data;
        this.next = next;
        this.prev = prev;
    }
}

// Create a Doubly Linked List
class LinkedList {
    constructor(initializer_list = null) {
        if (initializer_list) {
            // TODO;
        } else {
            this.head = null;
            this.tail = null;
        }
    }


    // ==================
    // Methods for LL
    // ==================
    
    // Insert at head

    // Insert at tail

    // Insert at index

    // Get at index

    // Delete at head

    // Delete at tail

    // Delete at index

    // Clear list

    // Print list
}